# レイアウトと動きの共通ルール（2026-09-20）

## レイアウト

| 条件 | 列数 | 列間 | 左右の最低余白 | セクション上下 | パネル内部 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 1200px以上 | 12 | 32px | 48px | 88px | 32px |
| 960〜1199px | 12 | 32px | 32px | 88px | 32px |
| 600〜959px | 8 | 24px | 32px | 64px | 24px |
| 360〜599px | 4 | 16px | 24px | 48px | 24px |
| 359px以下 | 4 | 16px | 16px | 48px | 16px |

本文最大1200px。ヘッダー・本文・フッターの外枠を共通化。`.layout-grid` とCSS変数を共通の定義にする。

PCの割付：FVは5/7、予約パネルは内部4/8、お知らせ3/9、診療カード4/4/4、写真と説明6/6、見出しと導入6/6、アクセス6/6。タブレットはFV4/4、予約パネル全幅、診療カード全幅の中で写真3/本文5、医院紹介4/4。スマホは原則4列全幅。

余白は8px刻みの `--s-*` を使用。4pxは日付と本文の間などの微調整に限定。文字サイズ・罫線・写真の曲率は余白スケールと別管理。ヘッダー内部のロゴ／ナビ／予約、時刻／曜日、アイコン／本文などは用途別の小さなレイアウトを使い、12列へ無理に割り付けない。

## 動き

| 役割 | 時間 | 距離／振る舞い |
| --- | --- | --- |
| 全画面オープニング | 約2800ms | 旧版のロゴと3シェイプ。1000ms保持、800msで背景をフェード。写真1600ms、コピー1100ms・160ms差。2900msで終了保証 |
| FV緑シェイプ | 7000ms周期 | PC・タブレット20px、スマホ12pxの上下浮遊 |
| 本文の初回出現 | 1100ms | 6px上へ移動しながら表示。同じ行160ms差、最大320ms |
| ボタン | 600ms | 右中央から色面が広がる。矢印だけ4px移動 |
| 文字リンク・メニュー | 600ms | 下線／色、メニューのフェードと8px移動 |

2026-09-21更新：旧版FVコピーの1100msと cubic-bezier(.22,1,.36,1) を出現の基準にする。FVコピーは不透明度だけ変化。全画面のロゴと装飾を旧版から復帰。クリック・スクロール・キー・フォーカスで即終了させない。初回描画に間に合わないスクリプト、直接リンク、途中位置の再読込、戻る操作では省略。画像デコードを待たない固定時間のため低速回線でも延長しない。

浮遊は画面外・非表示タブで停止。OSの動きを減らす設定では演出を省略。JavaScript未読込でも全内容を表示し、観測API未対応では本文を隠さない。移動先とフォーカス対象は出現待ちせず表示する。

## 調べた根拠とサイト向けの判断

- [Carbon: Motion overview](https://carbondesignsystem.com/elements/motion/overview/) は、操作応答と表現的な動きを分け、移動量・サイズに応じた長さと緩急を推奨。150/240/400/700msなど用途別の値を示す。
- [Material Components: Motion](https://github.com/material-components/material-components-android/blob/master/docs/theming/Motion.md) のduration tokensにも300/400msを含む段階がある。
- 初期の300msホバー・400ms出現は上記を出発点にしたが、9月21日のユーザー指定を優先し、旧版FVを基準に更新した。

## 検証

- 実効幅291/320/355/390/433/599/600/660/666/766/853/959/960/1024/1044/1066/1138/1333/1440/1600/2133pxで横はみ出し・見出し・予約・表の内部はみ出しなし。境界で列数4/8/12が切り替わることを実測。
- PC/スマホ/タブレットの先頭、スマホ診療案内、メニューとアンカー、文字・予約を隠さないロゴ表示、緑シェイプ浮遊と画面外停止、フォーカス時の色面と矢印をブラウザで確認。
- `node tests/design-motion.test.cjs`：入力で中断しない、時間終了、描画後の遅延読込・直接リンクの省略、同じ行の時間差、一度だけの出現、フォーカス、reduced motion、非表示タブ、印刷、API失敗・未対応時の復帰を仮想DOMで検証。
- HTML構造・ID・内部リンク・静的アセット参照、JS構文、indexと互換ページの一致を確認。
- 実機・他ブラウザ・実OS設定切替は未検証。旧デザインlegacy/は変更していない。
